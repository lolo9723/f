<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET uygulama düzeyinde tanımlıdır; aşağıdaki <processes> ile varsayılan ajan prosesinden alınır. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SMS, rehber, bildirim, mikrofon, kamera veya geniş depolama izni yoktur. -->

    <queries>
        <package android:name="com.android.chrome" />
        <package android:name="com.instagram.android" />
        <package android:name="com.canva.editor" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <!-- Android 11+: Ana süreç internete çıkamaz; yalnız görünür :web süreci internete çıkar. -->
        <processes>
            <deny-permission android:name="android.permission.INTERNET" />
            <process />
            <process android:process=":web">
                <allow-permission android:name="android.permission.INTERNET" />
            </process>
        </processes>

        <activity
            android:name=".FolderGrantActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".WebResearchActivity"
            android:exported="false"
            android:process=":web"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".MainActivityV3"
            android:exported="true"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".AgentAccessibilityServiceV4"
            android:exported="false"
            android:label="Yerel Ajan Görünür Otomasyon"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
