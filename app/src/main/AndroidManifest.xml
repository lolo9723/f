<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SMS, rehber, bildirim, mikrofon, kamera veya geniş depolama izni yoktur. -->

    <queries>
        <package android:name="com.android.chrome" />
        <package android:name="com.instagram.android" />
        <package android:name="com.canva.editor" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <processes>
            <deny-permission android:name="android.permission.INTERNET" />
            <process />
            <process android:process=":planner">
                <deny-permission android:name="android.permission.INTERNET" />
            </process>
            <process android:process=":web">
                <allow-permission android:name="android.permission.INTERNET" />
            </process>
        </processes>

        <uses-native-library android:name="libvndksupport.so" android:required="false" />
        <uses-native-library android:name="libOpenCL.so" android:required="false" />

        <provider
            android:name=".VmStateProvider"
            android:authorities="tr.edu.balikesir.yerelajan.vmstate"
            android:exported="false"
            android:grantUriPermissions="false" />

        <activity
            android:name=".FolderGrantActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".WebResearchActivity"
            android:exported="false"
            android:process=":web"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".ModelSetupActivity"
            android:exported="false"
            android:process=":web"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".ModelSetupActivityV2"
            android:exported="false"
            android:process=":web"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".LocalPlannerActivity"
            android:exported="false"
            android:process=":planner"
            android:excludeFromRecents="true"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".MainActivityV3"
            android:exported="true"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".AgentAccessibilityServiceV4"
            android:exported="false"
            android:label="Yerel Ajan Görünür Otomasyon"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
